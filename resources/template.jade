html
	head
		include head.jade
	body
		p
			em trntxt.uk
				small (beta)
		!= content

		if toStation != undefined and fromStation != undefined
			p
				a(href='/'+toStation+'/'+fromStation) Switch Direction

		include footer.jade